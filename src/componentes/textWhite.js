import { View, Text } from "react-native";

export default TextWhite = ({texto}) =>{
    return(
    <View>
        <Text style={{color:"white", fontSize: size || 15 }}>{texto}</Text>
    </View>
    );
} ;